name: upi_parser_ai
description: A Dart package for parsing UPI transactions and PII sanitization.
version: 0.1.0
homepage: https://github.com/yourusername/upi_parser_ai # Replace with your actual repo URL
publish_to: 'https://pub.dev'

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  http: ^1.1.0
  json_annotation: ^4.8.1

dev_dependencies:
  build_runner: ^2.4.0
  json_serializable: ^6.7.0
  lints: ^2.0.0
  test: ^1.21.0